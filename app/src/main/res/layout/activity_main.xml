<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

<!-- TODO Aumentar a fonte do texto para 30dp -->
    <TextView
        android:id="@+id/lbl_hello"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Olá jovens!"
        android:textSize="30dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:backgroundTint="#009900"
        android:text="Está tudo bem"
        android:layout_centerHorizontal="true"
        android:id="@+id/btn_ok" />

<!-- TODO Reposicionar o botão para ficar acima do btn_ok -->
    <Button
        android:id="@+id/btn_erro"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="#990000"
        android:layout_above="@+id/btn_ok"
        android:layout_centerHorizontal="true"
        android:text="Ops!!" />

</RelativeLayout>